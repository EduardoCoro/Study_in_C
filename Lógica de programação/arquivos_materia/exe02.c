/*
#include <stdio.h>

int main(){

    FILE *pFile;

    pFile = fopen("arquivo-dados.txt", "a+");

    if (pFile != NULL){

        char c;
        for (c = 'A' ; c <= 'Z' ; c++){
            fputc (c , pFile );
        }

        fclose (pFile);
    }

    return 0;
}

*/
